image: archlinux/base:latest

before_script:
    - pacman --noconfirm -Syu
    - pacman --noconfirm -S rust opencv
    - ln -s /usr/include/opencv4/opencv2 /usr/include/opencv2

stages:
    - build

cargo_build:
    stage: build
    script:
    - cargo build
    artifacts:
        paths:
        - pearls
